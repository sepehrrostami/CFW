{
  "name": "edge-relay",
  "private": true,
  "version": "1.0.0",
  "description": "A lightweight WebSocket-to-TCP relay for Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
